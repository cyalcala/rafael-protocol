{
  "name": "@rafael/snippet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target browser --minify",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@rafael/types": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0",
    "wrangler": "^3.0.0"
  }
}
